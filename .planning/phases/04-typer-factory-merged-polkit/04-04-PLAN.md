---
phase: 04-typer-factory-merged-polkit
plan: 04
type: execute
wave: 4
depends_on: [04-03]
files_modified:
  - logpose/main.py
  - README.md
autonomous: true
requirements: [CLI-05, PKG-08]
must_haves:
  truths:
    - "No `sys.exit(...)` call remains in `logpose/main.py`; all error exits use `raise typer.Exit(code=1)` and the one clean-quit path uses `raise typer.Exit()` (no code)."
    - "README.md has updated CLI examples using `logpose palworld <verb>` form for install, start, stop, status, edit-settings — matching the game-first dispatch shipped in Plan 04-02."
    - "The `import sys` statement is either removed (if no other `sys.*` reference exists after conversion) OR retained if `sys.stderr` is still used by `rich.print(..., file=sys.stderr)` call sites — whichever matches the final source."
    - "Byte-diff harness still green (4 tests passing) after the commit."
    - "Final grep audits pass: `@app.command` = 0, `sys.exit` = 0, hardcoded palworld literals inside `_build_game_app` body = 0."
  artifacts:
    - path: "logpose/main.py"
      provides: "Zero `sys.exit` calls; `typer.Exit` used throughout; import list reconciled."
      contains: "raise typer.Exit"
    - path: "README.md"
      provides: "Updated Palworld CLI examples in `logpose palworld <verb>` form (PKG-08 partial — Palworld half; ARK + migration note land in Phase 5/6)."
  key_links:
    - from: "logpose/main.py error paths"
      to: "Typer/Click exit-code propagation"
      via: "`raise typer.Exit(code=1)`"
      pattern: "raise typer\\.Exit\\(code=1\\)"
    - from: "README.md installation/usage sections"
      to: "`logpose palworld <verb>` CLI surface"
      via: "command examples under game-first dispatch"
      pattern: "logpose palworld"
---

<objective>
Finish Phase 4: convert every remaining `sys.exit(...)` call in `logpose/main.py` to the appropriate `typer.Exit` variant (CLI-05), update the README's Palworld usage examples to the `logpose palworld <verb>` form (PKG-08 partial — the Palworld half; ARK + migration note are Phase 5/6), and run the full end-of-phase grep audit.

Purpose: Close CLI-05 (final requirement left open in Phase 4) and deliver the Palworld slice of PKG-08 under the game-first CLI. Leave the codebase in a state where Phase 5 can slot `GAMES["ark"]` in with zero main.py scaffolding changes.

Output: `logpose/main.py` free of `sys.exit`; `README.md` updated; harness green; one atomic commit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-typer-factory-merged-polkit/04-CONTEXT.md
@.planning/phases/04-typer-factory-merged-polkit/04-RESEARCH.md
@.planning/phases/04-typer-factory-merged-polkit/04-01-PLAN.md
@.planning/phases/04-typer-factory-merged-polkit/04-02-PLAN.md
@.planning/phases/04-typer-factory-merged-polkit/04-03-PLAN.md
@logpose/main.py
@README.md
@tests/test_palworld_golden.py

<interfaces>
Exit conversion inventory (04-RESEARCH.md lines 560–571; some sites may already be converted depending on Plan 04-01/04-02 factory body edits — locate current sites via grep before editing):

| Original site (v0.1.19 lines; current lines may differ after Plans 04-01..04-03) | Function | Current `sys.exit(...)` | Target |
|----|----|----|----|
| `_get_template` | after `FileNotFoundError` | `sys.exit(1)` | `raise typer.Exit(code=1)` |
| `_run_command` | after `CalledProcessError`/`FileNotFoundError` | `sys.exit(1)` | `raise typer.Exit(code=1)` |
| `_create_settings_from_default` | after "Default configuration file not found" | `sys.exit(1)` | `raise typer.Exit(code=1)` |
| `_interactive_edit_loop` | user typed `quit` | `sys.exit(0)` | `raise typer.Exit()` (NO code — clean quit) |
| Factory `install` root check | `Path.home() == Path("/root")` | already `typer.Exit(code=1)` from Plan 04-01 — NO change |
| Factory `edit-settings` default-fallback parse fails | already `typer.Exit(code=1)` from Plan 04-01 — NO change |
| Factory `edit-settings` outer save try/except | already `typer.Exit(code=1)` from Plan 04-01 — NO change |

**Pitfall 6 discipline (04-RESEARCH lines 332–336):**
- `raise typer.Exit()` (no code) = clean quit → use it ONLY for `_interactive_edit_loop`'s `quit` path.
- `raise typer.Exit(code=1)` = error exit → use it for the other four sites.
- Do NOT use `raise typer.Exit(code=0)` anywhere (redundant; Typer exits 0 by default on successful command return).

**Import hygiene after conversion:**
- `rich.print(..., file=sys.stderr)` still uses `sys.stderr` in several places (see `_get_template`, `_run_command`, `_fix_steam_sdk`, `_create_settings_from_default`, factory `install`, factory `edit-settings`) — KEEP `import sys` if ANY `sys.stderr` reference remains. Grep `sys\.` in logpose/main.py after conversion:
  - `sys.exit` — must be 0 hits.
  - `sys.stderr` — expected to remain (multiple hits).
- If `sys` is still used, leave the import. If somehow no `sys.*` reference remains (unlikely given `sys.stderr` usage), remove the import.

README scope for PKG-08 (partial — Palworld only in this phase):

Current `README.md` almost certainly shows v0.1.19 invocations like `palworld-server-launcher install --port 8211 --start`. Update every Palworld usage example to the game-first form:
- `palworld-server-launcher install` → `logpose palworld install`
- `palworld-server-launcher start` / `stop` / `restart` / `status` / `enable` / `disable` / `update` / `edit-settings` → `logpose palworld <verb>`
- Update the entry-point references / pip install instructions to `pip install logpose-launcher` (distribution name per PKG-02) and the CLI binary name `logpose`.

**OUT OF SCOPE for this plan's README edit (to avoid dragging Phase 5/6 scope in):**
- ARK examples → Phase 5
- Migration note for v0.1.19 users → Phase 6 (PKG-08 remainder)
- Per-game firewall port reference for ARK → Phase 6
- Manual polkit cleanup instructions (`sudo rm /etc/polkit-1/rules.d/40-palserver.rules`) → Phase 6

If the README already has a "Commands" or "Usage" section: edit in place. If it has a one-shot example block: edit in place. Keep the structure minimal — this is a targeted Palworld-slice update, not a rewrite.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert remaining sys.exit sites to typer.Exit in logpose/main.py</name>
  <files>logpose/main.py</files>
  <action>
1. Grep current state to locate remaining sites:
   ```bash
   grep -n 'sys\.exit' logpose/main.py
   ```
   Expect 4 hits (helper-level `_get_template`, `_run_command`, `_create_settings_from_default`; interactive loop `sys.exit(0)`). If more or fewer are found, reconcile against the inventory table above before editing.

2. For each error-path site (`_get_template`, `_run_command`, `_create_settings_from_default`):
   - Replace `sys.exit(1)` with `raise typer.Exit(code=1)`.
   - Keep the preceding `rich.print(..., file=sys.stderr)` line as-is (message must still fire before the raise).

3. For `_interactive_edit_loop` quit path:
   - Replace `sys.exit(0)` with `raise typer.Exit()` (no code — intent is clean quit, per Pitfall 6).
   - The `console.print("Exiting without saving.")` that precedes it stays.

4. Confirm `import sys` is still needed:
   ```bash
   grep -n 'sys\.' logpose/main.py
   ```
   If any `sys.stderr` references remain (expected — multiple call sites), keep `import sys`. Otherwise remove it.

5. Final grep audit — MUST pass:
   ```bash
   ! grep -n 'sys\.exit' logpose/main.py                                 # zero sys.exit
   ! grep -E '^@app\.command' logpose/main.py                            # zero flat commands
   grep -c 'raise typer.Exit' logpose/main.py                            # >= 5 (4 new + factory's pre-existing from Plan 04-01)
   ```
   Also spot-check inside `_build_game_app`:
   ```bash
   awk '/^def _build_game_app/,/^def |^for _key|^@app\.callback/' logpose/main.py | grep -E '2394010|palserver\.service|"palworld"' || echo 'OK: no hardcoded palworld literals in factory body'
   ```
   Must print `OK: no hardcoded palworld literals in factory body` (the only acceptable appearances of those literals are inside the `GAMES = { "palworld": GameSpec(..., service_name="palserver", app_id=2394010, ...) }` construction and the registration loop's f-string, which is outside the factory body).
  </action>
  <verify>
    <automated>cd /root/personal/palworld-server-launcher &amp;&amp; pytest tests/test_palworld_golden.py -x &amp;&amp; python -c "
src = open('logpose/main.py').read()
assert 'sys.exit' not in src, f'sys.exit still present: count={src.count(\"sys.exit\")}'
# Count typer.Exit usage — must be >= 5
assert src.count('raise typer.Exit') >= 5, f'typer.Exit count low: {src.count(\"raise typer.Exit\")}'
# Confirm clean-quit variant with no code exists (interactive loop)
import re
assert re.search(r'raise typer\.Exit\(\)', src), 'clean-quit typer.Exit() missing'
# Confirm error variant exists
assert re.search(r'raise typer\.Exit\(code=1\)', src), 'error typer.Exit(code=1) missing'
print('OK: sys.exit fully converted')
" &amp;&amp; python -c "
from typer.testing import CliRunner
from logpose.main import app
r = CliRunner()
for argv in [['--help'], ['palworld', '--help'], ['palworld', 'install', '--help']]:
    res = r.invoke(app, argv)
    assert res.exit_code == 0, f'{argv}: exit {res.exit_code} out={res.output}'
print('OK: CLI matrix still green')
"</automated>
  </verify>
  <done>Zero `sys.exit` in `logpose/main.py`; `raise typer.Exit` count ≥ 5 (one `Exit()` clean-quit + 4+ `Exit(code=1)` error sites); CLI matrix still green; byte-diff harness 4/4 passing.</done>
</task>

<task type="auto">
  <name>Task 2: Update README Palworld examples to game-first CLI</name>
  <files>README.md</files>
  <action>
Read `README.md` in full first, then edit the Palworld slice:

1. Update every `palworld-server-launcher <verb>` example to `logpose palworld <verb>`. The nine verbs are `install`, `start`, `stop`, `restart`, `status`, `enable`, `disable`, `update`, `edit-settings`.

2. Update install instructions — the pip distribution name is `logpose-launcher` (PyPI) and the CLI binary name is `logpose`:
   - `pip install palworld-server-launcher` → `pip install logpose-launcher`
   - `pipx install palworld-server-launcher` → `pipx install logpose-launcher` (if the README uses pipx)

3. Update any binary-name references (e.g. `"$ palworld-server-launcher install"` in fenced code blocks) to `"$ logpose palworld install"`.

4. If the README describes the tool as "install and manage a Palworld dedicated server", update it to note that `logpose` is a multi-game launcher and the Palworld sub-command is demonstrated (do NOT add ARK examples here — Phase 5 covers ARK). A one-line parenthetical like "(ARK support ships in v0.2.0 under `logpose ark <verb>` — see the ARK section below)" is acceptable IF the README already has a stub ARK section; otherwise omit.

5. Do NOT add: migration notes for v0.1.19 users, per-game firewall port tables for ARK, manual polkit cleanup commands. Those land in Phase 6's PKG-08 completion.

6. Do NOT rewrite the README's structure. Targeted edits only.

7. Preserve the existing LICENSE reference, README tone, and any badges/images.
  </action>
  <verify>
    <automated>cd /root/personal/palworld-server-launcher &amp;&amp; python -c "
readme = open('README.md').read()
# Must have new examples
assert 'logpose palworld' in readme, 'logpose palworld examples missing'
# Must drop flat v0.1.19 binary from examples (one or two historical mentions in a 'migration' or 'history' section MAY survive — flag only if the count is > 2)
old_count = readme.count('palworld-server-launcher ')  # trailing space to match command invocations
assert old_count <= 2, f'too many stale palworld-server-launcher command invocations: {old_count}'
# Install instructions updated
assert 'logpose-launcher' in readme or 'pip install logpose' in readme, 'install instructions not updated'
print('OK: README Palworld slice updated')
"</automated>
  </verify>
  <done>README has `logpose palworld <verb>` examples; stale `palworld-server-launcher ` command invocations reduced to at most 2 (historical mentions tolerated); install instructions reference `logpose-launcher` or `logpose`.</done>
</task>

<task type="auto">
  <name>Task 3: Final end-of-phase grep audit + harness + commit</name>
  <files>.git</files>
  <action>
[BLOCKING] Full Phase 4 audit before committing:

1. Byte-diff harness: `pytest tests/test_palworld_golden.py -x` → 4 passed.
2. `python tests/test_palworld_golden.py` exits 0.
3. Grep audits on `logpose/main.py`:
   - `grep -c 'sys\.exit' logpose/main.py` → 0
   - `grep -cE '^@app\.command' logpose/main.py` → 0
   - `grep -c '@app.callback(' logpose/main.py` → 1
   - `grep -c 'def _build_game_app(' logpose/main.py` → 1
   - `grep -c 'app.add_typer(' logpose/main.py` → 1
   - `grep -c '_setup_polkit(Path.home().name)' logpose/main.py` → 1
   - `grep -c 'palserver.rules.template\|40-palserver.rules' logpose/main.py` → 0
4. Grep audit on repo:
   - `test ! -f logpose/templates/palserver.rules.template`
   - `test -f logpose/templates/40-logpose.rules.template`
   - `test -f tests/golden/40-logpose.rules.v0_2_0`
5. CLI smoke matrix via `CliRunner`: all 8 checks from Plan 04-02 Task 2 still green.

Stage exactly `logpose/main.py` and `README.md`; commit via HEREDOC:

```
chore(04-04): finish sys.exit → typer.Exit conversion + README Palworld CLI examples

Converts the last four sys.exit(...) call sites in logpose/main.py to
raise typer.Exit(code=1) (error paths) / raise typer.Exit() (clean quit
in _interactive_edit_loop). After this commit `grep sys.exit logpose/main.py`
returns zero lines — CLI-05 is fully closed.

Updates README.md Palworld usage examples to the game-first shape:
`logpose palworld <verb>` replaces every `palworld-server-launcher <verb>`
command invocation; pip install instructions updated to logpose-launcher.
ARK examples + migration notes + per-game firewall port reference remain
Phase 5/6 scope (PKG-08 partial — Palworld slice only here).

Closes: CLI-05 (fully), PKG-08 (partial — Palworld examples).

End-of-phase grep audit (all pass):
- sys.exit in logpose/main.py: 0
- @app.command decorators: 0
- @app.callback: 1
- _build_game_app factory: 1
- app.add_typer loop: 1
- palserver.rules.template references in code: 0

Byte-diff harness: 4 passed.
VM-gated success criteria (ROADMAP Phase 4 #2 install E2E, #3 pkcheck) are
deferred to Phase 5's VM sweep per 04-RESEARCH Assumption A6.
```
  </action>
  <verify>
    <automated>cd /root/personal/palworld-server-launcher &amp;&amp; pytest tests/test_palworld_golden.py -x &amp;&amp; python tests/test_palworld_golden.py &amp;&amp; python -c "
src = open('logpose/main.py').read()
import re
checks = {
    'sys.exit = 0': src.count('sys.exit') == 0,
    '@app.command = 0': len(re.findall(r'^@app\.command', src, re.MULTILINE)) == 0,
    '@app.callback = 1': src.count('@app.callback(') == 1,
    '_build_game_app = 1': src.count('def _build_game_app(') == 1,
    'app.add_typer loop': src.count('app.add_typer(') == 1,
    '_setup_polkit call-site': '_setup_polkit(Path.home().name)' in src,
    'no stale polkit refs': 'palserver.rules.template' not in src and '40-palserver.rules' not in src,
}
for k, v in checks.items():
    assert v, f'FAIL: {k}'
print('OK: all end-of-phase grep audits green')
" &amp;&amp; git log -1 --pretty=%s | grep -q "^chore(04-04): finish sys.exit" &amp;&amp; git diff HEAD~1 --name-only | sort > /tmp/p4-04-diff.txt &amp;&amp; python -c "
want = {'README.md', 'logpose/main.py'}
got = set(l.strip() for l in open('/tmp/p4-04-diff.txt') if l.strip())
assert got == want, f'want={want}, got={got}'
print('OK: commit scope = README.md + logpose/main.py only')
"</automated>
  </verify>
  <done>Phase 4 complete: harness 4/4 green; all grep audits pass; one atomic commit touching exactly `logpose/main.py` + `README.md`.</done>
</task>

</tasks>

<verification>
End-of-phase invariants:
- [BLOCKING] `pytest tests/test_palworld_golden.py -x` → 4 passed.
- `python tests/test_palworld_golden.py` exits 0.
- Grep: zero `sys.exit`, zero `@app.command`, exactly one `@app.callback`, exactly one `_build_game_app`, exactly one `app.add_typer` registration, zero stale polkit references in code.
- `logpose --help`, `logpose --version`, `logpose palworld --help`, `logpose palworld install --help`, `logpose palworld edit-settings --help` all exit 0.
- `palserver.service.template` untouched (PAL-02 continuous invariant).
- `40-palserver.rules` on disk not touched by code (POL-04).
- VM-gated criteria (Phase 4 ROADMAP SC#2 install E2E, SC#3 `pkcheck`) deferred to Phase 5 per Assumption A6.
</verification>

<success_criteria>
- Zero `sys.exit` in `logpose/main.py`; `typer.Exit` used with correct code semantics (error vs clean quit).
- README Palworld examples use `logpose palworld <verb>` form; install instructions reference `logpose-launcher` dist name.
- Byte-diff harness 4/4 green; all CLI smoke checks green.
- One atomic commit on main, exactly `logpose/main.py` + `README.md` modified.
- All Phase 4 requirement IDs (CLI-01..CLI-07, PAL-07, POL-01..POL-05 static, SET-03, PKG-08 partial, E2E-02 static) closed by the cumulative work of Plans 04-01..04-04.
</success_criteria>

<output>
After completion, create:
1. `.planning/phases/04-typer-factory-merged-polkit/04-04-SUMMARY.md` describing: final sys.exit → typer.Exit inventory closure, README edits, grep audit results.
2. `.planning/phases/04-typer-factory-merged-polkit/04-SUMMARY.md` — phase-level summary mirroring 03-SUMMARY.md structure: commits (4), requirement closure table, byte-diff harness status (3 → 4 tests), invariants preserved, handoff notes for Phase 5 (GAMES["ark"] slots in; arkmanager wrapper is pivoted scope; factory + merged polkit are ARK-ready; re-capture `40-logpose.rules.v0_2_0` golden when ARK is added).
</output>
