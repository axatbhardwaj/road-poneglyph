---
phase: 06-release-polish-pypi
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - dist/
  - build/
autonomous: true
requirements:
  - E2E-05
user_setup: []

must_haves:
  truths:
    - "pyproject.toml declares version = \"0.2.0\""
    - "pyproject.toml description reflects the multi-game scope (Palworld + ARK)"
    - "python -m build produces dist/logpose_launcher-0.2.0-py3-none-any.whl and dist/logpose_launcher-0.2.0.tar.gz"
    - "Wheel METADATA shows Name: logpose-launcher and Version: 0.2.0"
    - "Wheel contains logpose/templates/ (palserver.service.template, arkserver.service.template, 40-logpose.rules.template, logpose-ark.sudoers.template)"
    - "Byte-diff harness remains 6/6 green after the version bump commit"
  artifacts:
    - path: "pyproject.toml"
      provides: "Canonical packaging config with version = 0.2.0"
      contains: "version = \"0.2.0\""
    - path: "dist/logpose_launcher-0.2.0-py3-none-any.whl"
      provides: "Published artifact for TestPyPI + PyPI (Plans 06-03, 06-04)"
    - path: "dist/logpose_launcher-0.2.0.tar.gz"
      provides: "Source distribution for PyPI"
  key_links:
    - from: "pyproject.toml [project] version"
      to: "*.dist-info/METADATA Version: field"
      via: "setuptools build_meta backend"
      pattern: "Version: 0\\.2\\.0"
    - from: "pyproject.toml [tool.setuptools.package-data]"
      to: "logpose/templates/ files inside the wheel"
      via: "setuptools package-data glob"
      pattern: "logpose/templates/.*\\.template"
---

<objective>
Bump `logpose-launcher` from 0.1.19 to 0.2.0 and produce a clean local build whose wheel metadata advertises the new version. The wheel must carry all four runtime templates (Palworld service, ARK service, merged polkit rule, ARK sudoers) so the downstream TestPyPI + PyPI publishes (Plans 06-03, 06-04) push a correct artifact.

Purpose: E2E-05 requires `unzip -p dist/*.whl *.dist-info/METADATA` to show `Name: logpose-launcher` and `Version: 0.2.0`. Without this plan, the subsequent PyPI uploads would either republish 0.1.19 metadata or be rejected by PyPI's name/version duplication check.

Output: updated `pyproject.toml` at 0.2.0, verified wheel + sdist in `dist/`, byte-diff harness still green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-release-polish-pypi/06-CONTEXT.md
@.planning/phases/04-typer-factory-merged-polkit/04-SUMMARY.md
@.planning/phases/05-add-ark-entry-e2e-arkmanager-wrapper/05-SUMMARY.md
@pyproject.toml

<interfaces>
<!-- Current pyproject.toml top-level keys (extracted from file at plan-time). -->
<!-- Executor edits [project] version and description; leave everything else alone. -->

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "logpose-launcher"
version = "0.1.19"                        # ← bump to "0.2.0"
description = "A Linux dedicated game server launcher for Palworld and ARK: Survival Evolved — manages steamcmd, systemd, and polkit on Debian/Ubuntu."
readme = "README.md"
license = "MIT"
requires-python = ">=3.8"
classifiers = [
    "Programming Language :: Python :: 3",
    "Operating System :: POSIX :: Linux",
]
dependencies = ["typer>=0.9,<0.21", "rich>=13.0,<14"]

[project.scripts]
logpose = "logpose.main:app"

[tool.setuptools]
packages = ["logpose"]

[tool.setuptools.package-data]
"logpose" = ["templates/*"]
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bump version to 0.2.0 and confirm metadata surface</name>
  <files>pyproject.toml</files>
  <action>
    Edit `pyproject.toml` to change `version = "0.1.19"` to `version = "0.2.0"`. This is the ONLY field you change — `name`, `description` (already reads "Palworld and ARK: Survival Evolved" — leave it), `dependencies`, `requires-python`, classifiers, `[project.scripts]`, and `[tool.setuptools*]` are correct from Phase 1 and must not be touched.

    Do NOT add extra classifiers ("Development Status", "Topic :: Games/Entertainment", etc.) in this plan — keep the metadata diff minimal. Classifier expansion is a future-milestone polish item and is out of scope for v0.2.0 per PKG-02 (already complete).

    Rationale for this plan NOT editing description/classifiers: Phase 1 already landed the multi-game description (see pyproject.toml line 11 — "Palworld and ARK: Survival Evolved"). Re-editing invites drift. The only observable v0.1.19 → v0.2.0 delta the build-system MUST emit is the `Version:` header in `*.dist-info/METADATA`.

    After saving, run `grep -n '^version' pyproject.toml` to confirm exactly one line matches and shows `version = "0.2.0"`.
  </action>
  <verify>
    <automated>grep -c '^version = "0.2.0"$' pyproject.toml</automated>
    Expected output: `1`. If output is `0`, the edit did not land; if `2+`, there is a stray duplicate key.
  </verify>
  <done>
    `pyproject.toml` has exactly one `version = "0.2.0"` line. No other fields changed (confirm via `git diff pyproject.toml` — only the version line differs).
  </done>
</task>

<task type="auto">
  <name>Task 2: Clean build + verify wheel metadata (E2E-05)</name>
  <files>dist/, build/, *.egg-info/</files>
  <action>
    Produce a clean local build and verify the wheel carries the right metadata + templates.

    Steps:
    1. Ensure `build` is available: `python -m pip install --user --upgrade build` (or use a venv if the user prefers — no global-install guarantees are made by this plan; `pipx install build` also works). If `python -m build --help` already works, skip this step.
    2. Scrub stale artefacts (PKG-06 invariant — no stale `.egg-info` in tree):
       `rm -rf build/ dist/ *.egg-info/ logpose.egg-info/ logpose_launcher.egg-info/`
    3. Build both wheel + sdist: `python -m build`
    4. Verify exactly two artefacts exist with the expected filenames:
       `ls dist/` should show `logpose_launcher-0.2.0-py3-none-any.whl` and `logpose_launcher-0.2.0.tar.gz` (PyPI normalizes `logpose-launcher` → `logpose_launcher` in filenames — this is correct).
    5. Inspect wheel METADATA:
       `unzip -p dist/logpose_launcher-0.2.0-py3-none-any.whl 'logpose_launcher-0.2.0.dist-info/METADATA' | head -20`
       Must show (order may vary):
         - `Name: logpose-launcher`
         - `Version: 0.2.0`
         - `Requires-Python: >=3.8`
         - `Requires-Dist: typer<0.21,>=0.9`
         - `Requires-Dist: rich<14,>=13.0`
    6. Inspect wheel contents for template payload (package-data smoke-test):
       `unzip -l dist/logpose_launcher-0.2.0-py3-none-any.whl | grep 'logpose/templates/'`
       Must list all four templates: `palserver.service.template`, `arkserver.service.template`, `40-logpose.rules.template`, `logpose-ark.sudoers.template`.
    7. Confirm the CLI entry-point is declared:
       `unzip -p dist/logpose_launcher-0.2.0-py3-none-any.whl 'logpose_launcher-0.2.0.dist-info/entry_points.txt'`
       Must contain `logpose = logpose.main:app` under `[console_scripts]`.
    8. Run the byte-diff harness one more time to make sure the version bump did not perturb any template render: `pytest tests/ -x`. Must be 6 passed (4 palworld + 2 ark — inherited from Phase 5).

    Do NOT upload to TestPyPI or PyPI in this task — those are Plans 06-03 and 06-04, which are human-gated. Do NOT modify `.gitignore` — `dist/`, `build/`, `*.egg-info/` were added in Phase 1 (PKG-06).

    `dist/*` and `build/*` MUST NOT be committed (they are git-ignored per PKG-06; double-check with `git status --short` showing neither `dist/` nor `build/` as tracked after the build).
  </action>
  <verify>
    <automated>unzip -p dist/logpose_launcher-0.2.0-py3-none-any.whl 'logpose_launcher-0.2.0.dist-info/METADATA' | grep -E '^(Name|Version): ' | sort</automated>
    Expected exactly two lines:
      `Name: logpose-launcher`
      `Version: 0.2.0`
    Also run: `pytest tests/ -x` → 6 passed.
  </verify>
  <done>
    - `dist/logpose_launcher-0.2.0-py3-none-any.whl` and `dist/logpose_launcher-0.2.0.tar.gz` exist.
    - Wheel METADATA shows `Name: logpose-launcher` + `Version: 0.2.0` (E2E-05 ✅).
    - All four templates present under `logpose/templates/` inside the wheel.
    - `logpose = logpose.main:app` console script declared in entry_points.txt.
    - `pytest tests/ -x` → 6 passed (byte-diff harness green).
    - `git status --short` shows only `pyproject.toml` as modified (no `dist/`, `build/`, `*.egg-info/` tracked).
  </done>
</task>

</tasks>

<verification>
Phase-level checks this plan satisfies:
- E2E-05: wheel METADATA shows `Name: logpose-launcher` and `Version: 0.2.0` ✅
- PKG-06 invariant: no `.egg-info/` tracked after a clean build ✅
- PAL-02 / PAL-09 continuous invariant: byte-diff harness still 6/6 green after version bump ✅ (template rendering is independent of `[project] version`, but this plan pins it as an exit gate anyway)
</verification>

<success_criteria>
Measurable completion:
1. `grep -c '^version = "0.2.0"$' pyproject.toml` → 1.
2. `ls dist/logpose_launcher-0.2.0*.whl dist/logpose_launcher-0.2.0*.tar.gz` → both files listed, no other versions present.
3. `unzip -p dist/*.whl '*/METADATA' | grep -E '^(Name|Version): '` → exactly `Name: logpose-launcher` + `Version: 0.2.0`.
4. `pytest tests/ -x` → 6 passed.
</success_criteria>

<output>
After completion, create `.planning/phases/06-release-polish-pypi/06-01-SUMMARY.md` capturing:
- The exact single-line diff against `pyproject.toml`.
- Full `dist/` listing with file sizes.
- First 20 lines of `METADATA` (proving `Name:` + `Version:`).
- Template inventory from `unzip -l` (all four templates accounted for).
- `pytest tests/ -x` final line (`6 passed in Xs`).
- Any deviations (e.g., if `python -m build` had to be installed in a venv rather than globally — note that in Deviations).
- Handoff pointer: dist artefacts are the input to Plan 06-03 (TestPyPI) + Plan 06-04 (PyPI).
</output>
